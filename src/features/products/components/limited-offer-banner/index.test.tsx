import { render } from '@testing-library/react'

import { LimitedOfferBanner } from '.'

describe('limited-offer-banner', () => {
  it('should work', () => {
    render(<LimitedOfferBanner />)

    // Add your test here
  })
})
