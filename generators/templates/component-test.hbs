import { render } from '@testing-library/react'

import { {{ pascalCase name }} } from '.'

describe('{{ name }}', () => {
  it('should work', () => {
    render(<{{ pascalCase name }} />)

    // Add your test here
  })
})
