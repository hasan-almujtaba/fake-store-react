import { renderHook } from '@testing-library/react'

import { use{{ pascalCase name }} } from '../use-{{ kebabCase name }}'

describe('{{ lowerCase name }}', () => {
  it('should work', () => {
    const { result } = renderHook(() => use{{ pascalCase name }}())

    // Add your test
  })
})
